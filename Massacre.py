from board import *

def Massacre(goal_square, new_board):
    # create a suqare list according to goal_square

    while i < len(
        if either is occupied by black:
            goal_square.append(goals.pop())
            continue

            else:
                if check_move(goals.piece1):
                    if check_move(goals.piece2):
                        astar

                    else:
                        goal_square.append(goals.pop())


                elif check_move(goals.piece2):
                    if check_move(goals.piece1):
                        astar

                    else:
                    goal_square.append(goals.pop())
